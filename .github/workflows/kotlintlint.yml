# Lint is now handled by the Android Checks workflow (run-gradle-tests-on-every-push.yml).
# This stub is kept so that any branch protection rules referencing the `code-analysis`
# job name continue to resolve. It can be removed once those rules are updated.
name: KotlinLint

on:
  workflow_dispatch:

jobs:
  code-analysis:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Lint is now handled by the Android Checks workflow."
